Previously, pull requests were made by forking repositories from GitHub. Now, there is no need to fork. Instead, copy the SSH link from the Mehfilism community. Then, clone the link using the 'git clone' command. After that, you can commit normally and directly use the 'git push' command, without any request.
